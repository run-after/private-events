
<% if session[:id] %>
Logged in as:  
  <%= link_to "#{User.find(session[:id]).name}",
                              user_path(User.find(session[:id])) %><br>
  <%= link_to 'Log out', sign_out_path, :method => :delete %>
<% else %>
  <%= link_to 'Log in', sign_in_path %> 
<% end %>
| <%= link_to 'All Events', events_path %> | <%= link_to "New Event", new_event_path %> | <%= link_to "Sign up", new_user_path %>